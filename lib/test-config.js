'use strict'

module.exports = {
  tested: {
    // protocol from https://nodejs.org/docs/latest-v8.x/api/url.html#url_urlobject_protocol
    // is followed by a colon
    protocol: 'http:',
    hostname: 'localhost',
    port: 1111,
  },
  port: 1111,
}
