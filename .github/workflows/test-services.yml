name: Main
on:
  pull_request:

jobs:
  test-services:
    runs-on: ${{ matrix.os }}
    steps:
      - name: Checkout
        uses: actions/checkout@v3

      - name: Setup
        uses: ./.github/actions/setup
        with:
          node-version: 16

      - name: Service tests
        uses: ./.github/actions/service-tests
